To install required dependecies:

On Windows:
Run install_requirements.bat 

On other OS:
Open terminal and execute the following command (in current working directory):
pip install -r requirements.txt